class Client {
	int socket;
	// variabili per crypto


public
	int gestisciComando () {
		// login
		// talk
		// show
		// logout
	}
};



int main (int argc, char** argv)
{
    // controllo parametri ingresso

    // crea socket client, configura
    // connect to server

    // chiedi password per chiave pubblica

	// esegui show

	// crea thread
		1 -> // attendi comando client (cin)
		2 -> // attendi Server (cout)
}
